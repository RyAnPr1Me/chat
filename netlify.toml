[build]
  command = "npm ci && echo 'Build complete'"
  publish = "."

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/server"
  status = 200

[functions]
  directory = "netlify/functions"
